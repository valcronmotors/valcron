/** Website request capture only. Pipeline UI lives in DO Dealer, not this site. */
export const DEFAULT_REQUEST_STATE = "Nuevo Lead";
export const DEFAULT_LEAD_ORIGIN = "Web";

/** @deprecated Use DEFAULT_REQUEST_STATE. Kept for existing prospectos inserts. */
export const DEFAULT_CRM_STATE = DEFAULT_REQUEST_STATE;
